# {{name}}

A brief description of your toolkit.

## Overview

Explain what your toolkit does and what scientific domain it covers.

## Tools

### example_tool

Description of what this tool does.

**Parameters:**
- `input_value` (float): Description
- `option` (str, optional): Description

**Returns:**
- JSON string with results

**Example:**
```python
result = example_tool(42.0, option="test")
# Returns: {"status": "ok", "result": 84.0, ...}
```

### text_processor

Another example tool for text processing.

**Parameters:**
- `text` (str): Text to process
- `uppercase` (bool, optional): Convert to uppercase (default: True)

**Returns:**
- JSON string with processed text

## Citation

If you use this toolkit in your research, please cite:

```bibtex
@software{{{name}},
  author = {Your Name},
  title = { {{name}} },
  year = {2024},
  url = {https://scitoolkit.org/toolkit/{{name}}}
}
```